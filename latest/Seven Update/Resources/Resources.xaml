<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:SevenUpdate.Converters"
                    >
    <converters:FileSizeConverter x:Key="fileSizeConverter" />
    <converters:LocaleStringConverter x:Key="localeStringConverter" />
    <converters:BoolToStringConverter x:Key="is64BitConverter" />
    <converters:EnumToBoolConverter x:Key="enumConverter" />
    <converters:StringToLocaleConverter x:Key="stringLocaleConverter" />

    <DataTemplate x:Key="HeaderTemplateDescending">
        <DockPanel>
            <TextBlock HorizontalAlignment="Left" Text="{Binding}" />
            <Path x:Name="arrow" StrokeThickness="1" Fill="Gray" Data = "M 5,5 L 10,10 L 15,5 L 5,5" />
        </DockPanel>
    </DataTemplate>
    <DataTemplate x:Key="HeaderTemplateAscending">
        <DockPanel>
            <TextBlock HorizontalAlignment="Left" Text="{Binding}"/>
            <Path x:Name="arrow" StrokeThickness="1" Fill="Gray" Data = "M 5,10 L 15,10 L 10,5 L 5,10" />
        </DockPanel>
    </DataTemplate>

    <Style x:Key="leftAlignHeader" TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="5,0" />
    </Style>
</ResourceDictionary>