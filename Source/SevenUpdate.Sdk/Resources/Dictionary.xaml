<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:Microsoft.Windows.Controls;assembly=Windows.Shell">
	<Style x:Key="shadowedLabelStyle">
		<Setter Property="TextBlock.Foreground" Value="{x:Static SystemColors.ControlDarkBrush}"/>
		<Setter Property="FrameworkElement.Opacity" Value="0.8"/>
		<Setter Property="TextBlock.FontSize" Value="12"/>
		<Setter Property="TextBlock.FontStyle" Value="Italic"/>
		<Setter Property="TextBlock.Margin" Value="8,4,4,4"/>
	</Style>

	<Style TargetType="{x:Type Controls:InfoTextBox}">
		<Setter Property="FontSize" Value="14"/>
		<Setter Property="Margin" Value="5,2,2,2"/>
		<Setter Property="LabelStyle" Value="{StaticResource shadowedLabelStyle}"/>
	</Style>

	<Style x:Key="underlineTextBlock" TargetType="TextBlock">
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="TextDecorations" Value="Underline"/>
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style TargetType="{x:Type Image}">
		<Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor"/>
	</Style>

    <Style x:Key="backButton" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter x:Name="img" ContentTemplate="{TemplateBinding Content}" Opacity="1" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="true">
                            <Setter TargetName="img" Property="Content">
                                <Setter.Value>
                                    <OuterGlowBitmapEffect GlowColor="Black" GlowSize="5" Noise="0" Opacity=".5" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsDefaulted" Value="true">
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="img" Property="BitmapEffect">
                                <Setter.Value>
                                    <OuterGlowBitmapEffect GlowColor="Black" GlowSize="5" Noise="0" Opacity=".5" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="img" Property="Margin" Value="1,1,-1,-1" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="img" Property="Opacity" Value=".2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>