<Page x:Class="SevenUpdate.Pages.Options" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Controls="clr-namespace:SevenUpdate.Controls" mc:Ignorable="d" Title="Options" MinHeight="500" MinWidth="850" ShowsNavigationUI="False" d:DesignWidth="850" Loaded="Page_Loaded">
	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Resources/Dictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Page.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="24" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="622.761*" />
			<ColumnDefinition Width="94.682" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="239.34*" />
			<RowDefinition Height="44" />
		</Grid.RowDefinitions>
		<TextBlock HorizontalAlignment="Left" Name="tbHeading" Text="Choose how Seven Update can install updates" VerticalAlignment="Top" FontSize="16" Foreground="#FF003399" Grid.ColumnSpan="2" Grid.Column="1" Margin="0,12,0,0" Padding="5" />
		<TextBlock HorizontalAlignment="Left" Text="When your computer is online, Seven Update can automatically check for updates and download them using these settings." VerticalAlignment="Top" Padding="5" Grid.ColumnSpan="2" Grid.Column="1" Grid.Row="1" Margin="0,5" />
		<Rectangle Grid.ColumnSpan="4" Grid.Row="10" HorizontalAlignment="Stretch" Name="rectangle1" VerticalAlignment="Stretch" Fill="#FFF0F0F0" />
		<TextBlock HorizontalAlignment="Left" Padding="5" Text="Select the programs that will be updated with Seven Update." VerticalAlignment="Top" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="8" />
		<Line Stroke="#FFA0A0A0" VerticalAlignment="Top" Grid.Row="9" X1="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Page}}}" StrokeThickness="1" Grid.ColumnSpan="4" />
		<ScrollViewer VerticalScrollBarVisibility="Auto" Grid.ColumnSpan="4" Grid.Row="9" Grid.RowSpan="1">
			<ListView SelectionMode="Single" Name="listView" MinWidth="800" Thumb.DragDelta="Thumb_DragDelta" Controls:ListViewSorter.IsListViewSortable="True" Foreground="Black" BorderThickness="0" AlternationCount="2">
				<ListView.Resources>
					<ToolTip x:Key="itemToolTip">
						<TextBlock Text="{Binding Path=Description, Converter={StaticResource localeStringConverter}}" TextWrapping="Wrap" MaxWidth="500" />
					</ToolTip>
					<DataTemplate x:Key="itemCheckBox">
						<StackPanel Orientation="Horizontal">
							<CheckBox IsChecked="{Binding Path=IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
						</StackPanel>
					</DataTemplate>
					<DataTemplate x:Key="itemLink">
						<StackPanel Orientation="Horizontal">
							<TextBlock>
								<Hyperlink NavigateUri="{Binding Path=AppUrl}" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#FF0066CC">
									<TextBlock Text="{Binding Path=Name, Mode=OneWay, Converter={StaticResource localeStringConverter}}" />
								</Hyperlink>
							</TextBlock>
						</StackPanel>
					</DataTemplate>
				</ListView.Resources>
				<ListView.View>
					<GridView ColumnHeaderContainerStyle="{StaticResource HeaderTemplate}">
						<GridViewColumn CellTemplate="{StaticResource itemCheckBox}" Width="25" />
						<GridViewColumn CellTemplate="{StaticResource itemLink}" Header="Program" Width="350" />
						<GridViewColumn DisplayMemberBinding="{Binding Path=Publisher, Mode=OneWay, Converter={StaticResource localeStringConverter}}" Header="Publisher" Width="250" />
						<GridViewColumn Header="Architecture" Width="100" DisplayMemberBinding="{Binding Path=Is64Bit, Mode=OneWay, Converter={StaticResource is64BitConverter}}" />

					</GridView>
				</ListView.View>
				<ListView.ItemContainerStyle>
					<Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource alternatingListViewItemStyle}">
						<Setter Property="ToolTip" Value="{StaticResource itemToolTip}" />
					</Style>
				</ListView.ItemContainerStyle>
			</ListView>
		</ScrollViewer>
		<TextBlock HorizontalAlignment="Left" Name="tbListStatus" Padding="5" Text="Please wait, downloading the program list..." VerticalAlignment="Center" Grid.Column="1" Grid.Row="10" />
		<StackPanel Grid.Column="2" Grid.Row="10" HorizontalAlignment="Right" Name="stackPanel1" VerticalAlignment="Center" Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,0,10,0">
			<Button HorizontalContentAlignment="Center" IsDefault="True" MinHeight="23" MinWidth="75" Name="btnSave" Padding="5,1" VerticalContentAlignment="Center" Click="Save_Click">
				<StackPanel Orientation="Horizontal">
					<Image Source="{Binding Path=ShieldIcon}" Stretch="None" Margin="0,0,2,0" />
					<TextBlock Text="Save"></TextBlock>
				</StackPanel>
			</Button>
			<Button Content="Cancel" HorizontalContentAlignment="Center" IsDefault="False" MinHeight="23" MinWidth="75" Name="btnCancel" Padding="5,1" VerticalContentAlignment="Center" Margin="6,0,0,0" IsCancel="True" Click="Cancel_Click" />
		</StackPanel>
		<CheckBox Content="Give me recommended updates the same way i receive important updates" Grid.Column="1" Grid.Row="7" HorizontalAlignment="Left" IsChecked="True" Name="chkRecommendedUpdates" VerticalAlignment="Center" Grid.ColumnSpan="2" MinHeight="5" Margin="70,0,0,10" />
		<StackPanel Grid.Column="1" Grid.Row="6" HorizontalAlignment="Left" Name="stackPanel3" VerticalAlignment="Top" Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,10">
			<Image Height="48" Name="shieldIcon" Stretch="None" StretchDirection="UpOnly" Width="48" />
			<ComboBox IsDropDownOpen="False" IsReadOnly="True" MaxHeight="23" MinHeight="23" MinWidth="120" Name="cbAutoUpdateMethod" SelectedIndex="0" Margin="20,0,0,0" SelectionChanged="AutoUpdateMethod_SelectionChanged">
				<ComboBoxItem Content="Install updates automatically" />
				<ComboBoxItem Content="Download updates but let me choose to install them (recommended)" />
				<ComboBoxItem Content="Check for updates but let me choose to download and install them" />
				<ComboBoxItem Content="Never check for updates (not recommended)" />
			</ComboBox>
		</StackPanel>
		<StackPanel Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="2" HorizontalAlignment="Left" Name="stackPanel2" VerticalAlignment="Top" Orientation="Horizontal">
			<TextBlock Name="textBlock2" Padding="5" Text="Important updates" Foreground="#FF003399" Height="25.96" Width="108.557" />
			<Separator MinWidth="200" MinHeight="5" Height="5" Width="555" />
		</StackPanel>
		<Line Stroke="#FFA0A0A0" VerticalAlignment="Top" Grid.Row="10" X1="{Binding Path=ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Page}}}" StrokeThickness="1" Grid.ColumnSpan="4" />
	</Grid>
</Page>